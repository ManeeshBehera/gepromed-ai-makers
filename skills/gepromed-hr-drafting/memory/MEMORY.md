# GEPROMED HR Drafting — MEMORY

Self-updating house memory. The skill **loads this first** and applies everything
below. It **appends** here (via `scripts/memory_update.py` or a `📝 MEMORY UPDATE`
block) whenever the RAF/team member teaches it something durable. Priority: explicit
user instruction (RAF) > this file > references. **No memory entry ever overrides
ZERO-INVENTION** — store only RAF-validated, reusable company facts and house
decisions.

> This is a **company** memory, shared across all team members. Do **not** store an
> employee's personal data, a named person's salary, or unconfirmed legal text.
> Store only durable, RAF-validated company facts (CCN, working time, standard
> sections) and house decisions safe to reuse.

How entries are formatted: each line is one durable rule, prefixed with `FR:`,
`EN:`, or `BOTH:` where language-specific. The script keeps them de-duplicated and
dated in the Correction log.

---

## Approved company facts
<!-- RAF-validated, reusable facts (CCN/IDCC, working time, default location). Empty values until confirmed. -->
- BOTH: The applicable convention collective (CCN/IDCC), working time, and trial periods are NOT yet confirmed — keep `[à confirmer par le RAF]` until the RAF supplies them.
- BOTH: Default location reference is Strasbourg (confirm exact site/address with the RAF).
- BOTH: GEPROMED is a non-profit; certified ISO 9001 / ISO 13485 / Qualiopi (employer-brand context only).

## House-style decisions
<!-- Durable choices about structure, wording, formatting. Seeded; extend on use. -->
- BOTH: Every output ends with the ⚠️ VALIDATION FLAG + HR-legal disclaimer naming the RAF.
- BOTH: Never assert a Code du travail article number or a CCN clause — bracket and name the source.
- BOTH: Job offers are non-discriminatory and inclusive; flag doubtful wording for the RAF's equality check.
- FR: Job titles use an inclusive form (e.g. "H/F" or a neutral title) — exact form `[à confirmer par le RAF]`.
- EN: Job titles use an inclusive form (e.g. "M/F" or a neutral title) — exact form `[RAF to confirm]`.

## Recurring recipients & context
<!-- Standard sections, recurring teams/pillars, HR contact. Empty until learned. -->
- (none yet)

## Approved phrasings / snippets
<!-- Reusable, RAF-approved sentences (e.g. the equal-opportunity statement). Empty until learned. -->
- (none yet)

## Do / Don't learned
<!-- Specific things to do or avoid, learned from RAF corrections. Empty until learned. -->
- BOTH: In HR-legal answers, frame the questions/rules to verify — never conclude "this is legal" or "you must do X".

## Correction log
<!-- Dated record of what changed and why. Appended automatically. -->
- 2026-06-20 — Seed file created with initial HR house defaults and zero-invention guardrails.
