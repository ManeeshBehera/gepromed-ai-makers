# GEPROMED Management Review Deck — MEMORY

Self-updating house memory. The skill **loads this first** and applies everything
below. It **appends** here (via `scripts/memory_update.py` or a `📝 MEMORY UPDATE`
block) whenever the RQ/team member teaches it something durable. Priority: explicit
user instruction (RQ) > this file > references. **No memory entry ever overrides
ZERO-INVENTION** — store only durable, reusable house facts, **never KPI values**.

> This is a **company** memory, shared across all team members. Do **not** store
> actual KPI values, audit findings, satisfaction scores, or any figure — those
> change every review and must come from the RQ's source documents each time.
> Store only durable house facts: KPI *names*, section order, process list,
> standard period naming, audience defaults.

How entries are formatted: each line is one durable rule, prefixed with `FR:`,
`EN:`, or `BOTH:` where language-specific. The script keeps them de-duplicated and
dated in the Correction log.

---

## Approved QMS facts
<!-- Durable, reusable facts: process list, KPI NAMES (not values), QMS scope wording. Empty values until confirmed. -->
- BOTH: GEPROMED is ISO 9001 certified; the management review follows ISO 9001 §9.3.
- BOTH: The four core processes often reviewed: Testing, Education, Clinical Research, Explant Analysis (confirm exact QMS process names with the RQ).
- BOTH: KPI **values**, audit results, and decisions are NEVER stored here — always come from the RQ each review.

## House-style decisions
<!-- Durable choices about structure, wording, formatting. Seeded; extend on use. -->
- BOTH: Every output ends with the ⚠️ VALIDATION FLAG line naming the RQ.
- BOTH: Deck section order follows `references/iso-9001-9.3.md`.
- BOTH: Bracketed placeholders are rendered in orange on slides so the RQ spots them.
- FR: Title slide reads "Revue de direction — [période]"; status "PROJET — à valider par le RQ".
- EN: Title slide reads "Management Review — [period]"; status "DRAFT — to be validated by the RQ".

## Recurring recipients & context
<!-- Audience defaults, standard period naming, recurring source docs (names only). Empty until learned. -->
- (none yet)

## Approved phrasings / snippets
<!-- Reusable, RQ-approved sentences (e.g. a standard scope sentence). Empty until learned. -->
- (none yet)

## Do / Don't learned
<!-- Specific things to do or avoid, learned from RQ corrections. Empty until learned. -->
- BOTH: Never put a brand proof point (e.g. "+1150 trained") on a KPI slide as a current result.

## Correction log
<!-- Dated record of what changed and why. Appended automatically. -->
- 2026-06-20 — Seed file created with initial QMS house defaults and zero-invention guardrails.
