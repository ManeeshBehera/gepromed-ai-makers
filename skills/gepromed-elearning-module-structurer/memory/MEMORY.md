# GEPROMED E-learning Structurer Skill — MEMORY

Self-updating instructional-design memory. The skill **loads this first** and
applies everything below. It **appends** here (via `scripts/memory_update.py` or a
`📝 MEMORY UPDATE` block) whenever a GEPROMED team member teaches it something
durable. Priority: explicit user instruction > this file > references.

> This is a **company** memory, shared across all team members. Do not store
> secrets, unpublished assessment answers, or one-off facts. Store only durable
> house standards, naming, recurring specialties, and approved structural patterns.

How entries are formatted: each line is one durable rule, prefixed with `FR:`,
`EN:`, or `BOTH:` where language-specific. The script keeps them de-duplicated and
dated in the Correction log.

---

## Glossary additions
<!-- Approved terms / spellings / things never to translate. Seeded; extend on use. -->
- BOTH: Brand name is "GEPROMED" in titles/wordmark, "Gepromed" in running text.
- BOTH: "René Kieny Education Center" — keep this exact English name in all languages.
- BOTH: The Education pillar is this skill's anchor; modules can link to the other three pillars.

## House-style decisions
<!-- Durable choices about structure, objectives, assessment. Seeded; extend on use. -->
- BOTH: Objectives are Bloom-aligned, action-verb, observable; never "understand/know/be familiar with".
- BOTH: Match objective Bloom level to the learner (residents → recall/apply; clinicians → analyse/evaluate).
- BOTH: Hierarchy is modules → chapters → sections; assessment maps to objectives (constructive alignment).
- BOTH: Default ~3–5 chapters per module, 2–5 sections per chapter; adapt to the topic.
- BOTH: Clinical specifics are [SME] placeholders or [bracketed]; never invented.
- BOTH: Durations are estimates (`~`); pass thresholds are [bracketed] for the Education team.

## Recurring recipients & context
<!-- Specialties, course names, naming conventions. Empty until learned. -->
- (none yet)

## Approved phrasings / snippets
<!-- Reusable objective stems / checkpoint formats. Empty until learned. -->
- (none yet)

## Do / Don't learned
<!-- Specific things to do or avoid, learned from corrections. Empty until learned. -->
- (none yet)

## Correction log
<!-- Dated record of what changed and why. Appended automatically. -->
- 2026-06-20 — Seed file created with initial instructional-design defaults.
