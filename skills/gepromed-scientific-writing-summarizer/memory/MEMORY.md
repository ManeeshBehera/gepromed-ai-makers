# GEPROMED Scientific Writing Skill — MEMORY

Self-updating scientific house-style memory. The skill **loads this first** and
applies everything below. It **appends** here (via `scripts/memory_update.py` or a
`📝 MEMORY UPDATE` block) whenever a GEPROMED team member teaches it something
durable. Priority: explicit user instruction > this file > references.

> This is a **company** memory, shared across all team members. Do not store
> unpublished/embargoed data, confidential peer-review content, secrets, or one-off
> facts. Store only durable house-style preferences, terminology, citation-style
> conventions, and recurring journal/context information.

How entries are formatted: each line is one durable rule, prefixed with `FR:`,
`EN:`, or `BOTH:` where language-specific. The script keeps them de-duplicated and
dated in the Correction log.

---

## Glossary additions
<!-- Approved terms / spellings / things never to translate. Seeded; extend on use. -->
- BOTH: Brand name is "GEPROMED" in a wordmark context, "Gepromed" in running text.
- BOTH: Define every abbreviation on first use; keep device/anatomical terms exact.
- BOTH: "Explant analysis" is the standard term for examining retrieved devices.

## House-style decisions
<!-- Durable choices about register, citation handling, structure. Seeded; extend on use. -->
- BOTH: NEVER fabricate data or citations. Every reference slot is [CITATION NEEDED]; every unconfirmed number is [bracketed].
- BOTH: No citation automation — all references are human-verified by the author/RQ.
- BOTH: Summaries are faithful: only what the source says, with its hedging and limitations.
- BOTH: Drafts stay within supplied findings; hedge appropriately (association ≠ causation).
- BOTH: Neutral, non-promotional tone; no marketing proof points used as study data.
- BOTH: Detect mode (Summarize MINIMAL / Draft FULL) and run intake only for Draft.

## Recurring recipients & context
<!-- Target journals, citation styles, study acronyms, partners. Empty until learned. -->
- (none yet)

## Approved phrasings / snippets
<!-- Reusable hedged constructions / section openers. Empty until learned. -->
- (none yet)

## Do / Don't learned
<!-- Specific things to do or avoid, learned from corrections. Empty until learned. -->
- (none yet)

## Correction log
<!-- Dated record of what changed and why. Appended automatically. -->
- 2026-06-20 — Seed file created with initial scientific-integrity defaults.
