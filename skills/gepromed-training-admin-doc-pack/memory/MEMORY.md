# GEPROMED Training Admin Doc Pack — MEMORY

Self-updating house-style memory. The skill **loads this first** and applies
everything below. It **appends** here (via `scripts/memory_update.py` or a
`📝 MEMORY UPDATE` block) whenever a GEPROMED team member teaches it something
durable. Priority: explicit user instruction > this file > references.

> This is a **company** memory, shared across all team members. **Never** store
> participant names, DPO contacts, retention periods, or any one-off session data —
> these are not durable preferences and may be personal data. Store only durable
> layout/house-style choices and recurring context.

How entries are formatted: each line is one durable rule, prefixed with `FR:`,
`EN:`, or `BOTH:` where language-specific. The script keeps them de-duplicated
and dated in the Correction log.

---

## Glossary additions
<!-- Approved terms / spellings. Seeded; extend on use. -->
- BOTH: "feuille d'émargement" = the attendance/signing sheet; EN "attendance sheet".
- BOTH: "notice RGPD" / "mention RGPD" = the data-protection information notice.
- BOTH: "René Kieny Education Center" — keep this exact English name for the venue.

## House-style decisions
<!-- Durable choices about structure, wording, formatting. Seeded; extend on use. -->
- BOTH: Attendance sheet has signature matin + après-midi columns by default.
- BOTH: Badges omit the organisation line when it is missing (never bracket on a badge).
- BOTH: The RGPD notice always carries the DPO validation footer.
- BOTH: DPO email and retention period are always bracketed unless explicitly supplied — never invented.

## Recurring recipients & context
<!-- Recurring venues, audiences, partners. Empty until learned. -->
- (none yet)

## Approved phrasings / snippets
<!-- Reusable, pre-approved notice/sheet wording. Empty until learned. -->
- (none yet)

## Do / Don't learned
<!-- Specific things to do or avoid, learned from corrections. Empty until learned. -->
- (none yet)

## Correction log
<!-- Dated record of what changed and why. Appended automatically. -->
- 2026-06-20 — Seed file created with initial admin-pack house-style defaults.
