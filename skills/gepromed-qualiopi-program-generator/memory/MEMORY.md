# GEPROMED Qualiopi Program Generator — MEMORY

Self-updating house-style memory. The skill **loads this first** and applies
everything below. It **appends** here (via `scripts/memory_update.py` or a
`📝 MEMORY UPDATE` block) whenever a GEPROMED team member teaches it something
durable. Priority: explicit user instruction > this file > references.

> This is a **company** memory, shared across all team members. Do not store
> secrets, certificate numbers, prices, or one-off session details. Store only
> durable house-style preferences, terminology, and recurring program patterns.

How entries are formatted: each line is one durable rule, prefixed with `FR:`,
`EN:`, or `BOTH:` where language-specific. The script keeps them de-duplicated
and dated in the Correction log.

---

## Glossary additions
<!-- Approved terms / spellings / regulatory wording. Seeded; extend on use. -->
- BOTH: "René Kieny Education Center" — keep this exact English name for the simulation site.
- BOTH: Use "modalités pédagogiques" / "delivery methods" and "modalités d'évaluation" / "assessment methods" — the RNQ wording.
- BOTH: GEPROMED is "organisme de formation certifié Qualiopi" — never claim Qualiopi for the program itself, only the organisme.
- BOTH: Sanction par défaut = "Attestation de fin de formation" + "Certificat de réalisation".

## House-style decisions
<!-- Durable choices about structure, wording, formatting. Seeded; extend on use. -->
- BOTH: Objectives must use operational action verbs (réaliser, identifier, appliquer, évaluer); never "comprendre/connaître/sensibiliser" alone.
- BOTH: Evaluation must map to the objectives; always include a satisfaction questionnaire (chaud + froid).
- BOTH: Accessibilité handicap is always rendered as a process (référent handicap, étude au cas par cas), never as a fixed promise.
- BOTH: Prérequis is never blank — write "Aucun prérequis" if none.
- BOTH: Every unconfirmed price/date/rate is bracketed for the RQ; never invented.

## Recurring recipients & context
<!-- Recurring actions, audiences, partners, and how they are framed. Empty until learned. -->
- (none yet)

## Approved phrasings / snippets
<!-- Reusable, pre-approved sentences for common blocks. Empty until learned. -->
- (none yet)

## Do / Don't learned
<!-- Specific things to do or avoid, learned from corrections. Empty until learned. -->
- (none yet)

## Correction log
<!-- Dated record of what changed and why. Appended automatically. -->
- 2026-06-20 — Seed file created with initial Qualiopi house-style defaults.
