# GEPROMED Editorial Calendar Skill — MEMORY

Self-updating planning memory. The skill **loads this first** and applies
everything below. It **appends** here (via `scripts/memory_update.py` or a
`📝 MEMORY UPDATE` block) whenever a GEPROMED communication-function member
teaches it something durable. Priority: explicit user instruction > this file >
references.

> This is a **company** memory, shared across all team members. Do not store
> secrets, embargoed data, or patient-identifying data. Store only durable
> cadence, slots, themes, and recurring events.

How entries are formatted: each line is one durable rule, prefixed with `FR:`,
`EN:`, or `BOTH:` where language-specific. The script keeps them de-duplicated
and dated in the Correction log.

---

## Cadence & slots
<!-- Posting frequency, fixed days/slots per channel. Seeded; extend on use. -->
- BOTH: Default LinkedIn cadence = 2 posts/week (Tuesday + Thursday).
- BOTH: Only the current week is "Fixed"; the rest of the month is "Adjustable".
- BOTH: Website/blog ≈ 1–2 per month; newsletter ≈ 1 per month.

## Recurring themes & events
<!-- Standing themes, series, and events with how to plan around them. Seeded. -->
- BOTH: Vascular Bootcamp weeks get a 3-post arc (announce / build-up / recap).
- BOTH: A balanced month touches all four pillars; never two same-pillar posts back-to-back.

## House-style decisions
<!-- Durable planning choices. Seeded; extend on use. -->
- BOTH: Owner is always a role (e.g. Comms), never a named individual.
- BOTH: Never invent an event date; bracket it until confirmed.

## Approved phrasings / snippets
<!-- Reusable working titles / theme labels. Empty until learned. -->
- (none yet)

## Do / Don't learned
<!-- Specific things to do or avoid, learned from corrections. Empty until learned. -->
- (none yet)

## Correction log
<!-- Dated record of what changed and why. Appended automatically. -->
- 2026-06-20 — Seed file created with initial cadence and two-layer defaults.
