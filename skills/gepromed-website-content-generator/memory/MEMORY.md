# GEPROMED Website Content Skill — MEMORY

Self-updating house-style memory. The skill **loads this first** and applies
everything below. It **appends** here (via `scripts/memory_update.py` or a
`📝 MEMORY UPDATE` block) whenever a GEPROMED team member teaches it something
durable. Priority: explicit user instruction > this file > references.

> This is a **company** memory, shared across all team members. Do not store
> secrets, embargoed/unpublished data, or one-off facts. Store only durable
> house-style preferences, approved phrasings, terminology, and recurring page
> context (keywords, page names, slugs).

How entries are formatted: each line is one durable rule, prefixed with `FR:`,
`EN:`, or `BOTH:` where language-specific. The script keeps them de-duplicated
and dated in the Correction log.

---

## Glossary additions
<!-- Approved terms / spellings / things never to translate. Seeded; extend on use. -->
- BOTH: Brand name is "GEPROMED" in the wordmark/header, "Gepromed" in running text.
- BOTH: "René Kieny Education Center" — keep this exact English name on all language versions.
- BOTH: The four pillars are Testing · Education · Clinical Research · Explant Analysis.
- BOTH: "Satellite" is internal GEPROMED software — never expose on public pages without confirmation.

## House-style decisions
<!-- Durable choices about structure, wording, formatting. Seeded; extend on use. -->
- BOTH: Exactly one H1 per page; descriptive H2/H3 hierarchy, no skipped levels.
- BOTH: Meta title ends "| GEPROMED", ≤60 characters.
- BOTH: Meta description 120–158 characters, true, with a real proof point where it fits.
- BOTH: Intro must answer the page's core question in its first 1–2 sentences (GEO).
- BOTH: Anchor every page to one of the four pillars and make the patient-safety stake explicit.
- BOTH: Proof points used on a page must be true and confirmed; bracket any new figure or partner.

## Recurring recipients & context
<!-- Page names, slugs, target keyword sets, partners. Empty until learned. -->
- (none yet)

## Approved phrasings / snippets
<!-- Reusable, pre-approved sentences/CTAs for common pages. Empty until learned. -->
- (none yet)

## Do / Don't learned
<!-- Specific things to do or avoid, learned from corrections. Empty until learned. -->
- (none yet)

## Correction log
<!-- Dated record of what changed and why. Appended automatically. -->
- 2026-06-20 — Seed file created with initial house-style defaults.
