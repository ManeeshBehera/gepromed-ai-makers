# GEPROMED ISO Gap Analysis Skill — MEMORY

Self-updating QMS-convention memory. The skill **loads this first** and applies
everything below. It **appends** here (via `scripts/memory_update.py` or a
`📝 MEMORY UPDATE` block) whenever a GEPROMED team member teaches it something
durable. Priority: explicit user instruction > this file > references.

> This is a **company** memory, shared across all team members. Do not store the
> standards' copyrighted text, individual audit findings, secrets, or one-off facts.
> Store only durable conventions: severity scale, document maps, QMS context, owners.

How entries are formatted: each line is one durable rule, prefixed with `FR:`,
`EN:`, or `BOTH:` where language-specific. The script keeps them de-duplicated and
dated in the Correction log.

---

## Glossary additions
<!-- Approved terms / framings / things never to translate. Seeded; extend on use. -->
- BOTH: Brand name is "GEPROMED" in wordmarks, "Gepromed" in running text.
- BOTH: GEPROMED is certified to ISO 9001 and ISO 13485 (plus Qualiopi for training).
- BOTH: The ISO standards are copyrighted — never store or reproduce their clause text.

## House-style decisions
<!-- Durable choices about format, severity, conventions. Seeded; extend on use. -->
- BOTH: Severity scale is Critical / Major / Minor / Editorial.
- BOTH: Default owner for gap rows is the RQ (Responsable Qualité); may delegate.
- BOTH: Every output carries the ⚠ RQ-validation flag; never assert conformity/non-conformity.
- BOTH: Two versions/documents supplied → MINIMAL diff (no intake); summarize-updates → LIGHT (ask scope only).
- BOTH: Map ISO 13485 to ISO 9001 by intent, not by clause number.

## Recurring recipients & context
<!-- Recurring procedures, document maps, owners. Empty until learned. -->
- (none yet)

## Approved phrasings / snippets
<!-- Reusable, pre-approved summary/impact phrasings. Empty until learned. -->
- (none yet)

## Do / Don't learned
<!-- Specific things to do or avoid, learned from corrections. Empty until learned. -->
- (none yet)

## Correction log
<!-- Dated record of what changed and why. Appended automatically. -->
- 2026-06-20 — Seed file created with initial QMS and gap-analysis defaults.
